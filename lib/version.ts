import packageJson from '../package.json';

/** Semantic version — source of truth: package.json */
export const APP_VERSION = packageJson.version;

export const APP_TITLE = 'IG-Scout AI (Web Scraper)';
